# Trivy ignore list for accepted residual vulnerabilities.
#
# Currently empty by intent: the Dockerfile upgrades Alpine packages at build
# time, so the previously accepted libexpat and OpenSSL findings (fixed in
# expat 2.8.4-r0 and openssl 3.5.8-r0) no longer occur. Re-add entries here
# ONLY with a CVE comment, affected/fixed versions, compensating controls,
# and a re-evaluation trigger. Never add an entry to make a gate pass
# without all four.
